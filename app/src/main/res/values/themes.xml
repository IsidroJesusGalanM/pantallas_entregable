<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Ambigoo_pantalla_preguntas" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Ambigoo_pantalla_preguntas" parent="Base.Theme.Ambigoo_pantalla_preguntas" />

    <style name="custom_radio_button">
        <item name="android:layout_margin">4dp</item>
        <item name="android:drawableStart">@drawable/bg_radio_button</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:button">@null</item>
        <item name="android:background">@drawable/bg_radio_button_rectangule</item>
        <item name="android:padding">20dp</item>
        <item name="android:elevation">2dp</item>
    </style>
    
    <style name="custom_button_orange">
        <item name="android:background">@drawable/bg_buttons_orange</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>